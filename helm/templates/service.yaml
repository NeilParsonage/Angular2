apiVersion: v1
kind: Service
metadata:
  name: {{ include "fhi.fullname" . }}
  labels:
    {{- include "fhi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fhi.selectorLabels" . | nindent 4 }}
